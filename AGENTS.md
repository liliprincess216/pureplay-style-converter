# 版本保留约定

- 用户确认的生成器版本保存在 Git 标签 `v25-archive`，不得删除、移动、覆盖该标签，除非用户明确要求。
- 后续修改在 `codex/` 前缀的开发分支进行，不改变存档版本。
- 用户未明确要求修改生成算法、人物识别、滑动转场或下载逻辑时，保持这些行为不变；仅调整用户指定的部分。
- 保持本地预览工作流。未经用户要求，不发布网站。
- `outputs/` 中的归档压缩包是备份，不是开发源文件，不要覆盖已有版本备份。
